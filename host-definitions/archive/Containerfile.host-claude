ARG BASE_IMAGE=ghcr.io/doctorjei/kanibako-host:latest
FROM $BASE_IMAGE

USER root

COPY packages/agent-claude/ /tmp/agent-claude
RUN pip install --break-system-packages /tmp/agent-claude \
    && rm -rf /tmp/agent-claude

USER agent
