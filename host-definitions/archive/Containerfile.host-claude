ARG BASE_IMAGE=ghcr.io/doctorjei/kanibako-host:latest
FROM $BASE_IMAGE

USER root

COPY packages/plugin-claude/ /tmp/plugin-claude
RUN pip install --break-system-packages /tmp/plugin-claude \
    && rm -rf /tmp/plugin-claude

USER agent
